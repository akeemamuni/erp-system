# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @erp-system/erp-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t erp-system-erp-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system erp-system-erp-api && \
          adduser --system -G erp-system-erp-api erp-system-erp-api

COPY apps/erp-api/dist erp-system-erp-api/
COPY apps/erp-api/package.json erp-system-erp-api/
RUN chown -R erp-system-erp-api:erp-system-erp-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix erp-system-erp-api --omit=dev -f install

CMD [ "node", "erp-system-erp-api" ]
